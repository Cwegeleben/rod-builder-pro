/*
  NO-OP PLACEHOLDER MIGRATION (rolled back in production)
  -------------------------------------------------------
  Original attempt altered TemplateVersion.dataJson (TEXT -> JSON) and
  redefined several tables. It failed and was resolved as rolled back.
  This file intentionally contains no schema changes to keep the
  migration sequence consistent across environments.
*/

-- (no statements)
