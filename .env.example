SHOPIFY_API_KEY=""
SHOPIFY_API_SECRET=""

# Base URL when running locally without the Shopify CLI (optional)
SHOPIFY_APP_URL="http://localhost:3000"

# Comma-separated scopes for local runs (optional)
SCOPES="write_products,read_metaobjects,write_metaobjects"

# Enable smoke validation routes (Fly-only); returns 404 when disabled
ENABLE_SMOKES=""
# Token required to call smoke routes (query param token= or Authorization: Bearer)
SMOKE_TOKEN=""

# Optional: token for price/availability refresh route
PRICE_REFRESH_TOKEN=""