SHOPIFY_API_KEY=""

# Enable smoke validation routes (Fly-only); returns 404 when disabled
ENABLE_SMOKES=""
# Token required to call smoke routes (query param token= or Authorization: Bearer)
SMOKE_TOKEN=""

# Optional: token for price/availability refresh route
PRICE_REFRESH_TOKEN=""