name: Deploy — Production

on:
  push:
    branches: [production]

permissions:
  contents: write
  issues: write
  pull-requests: write
  id-token: write

jobs:
  release:
    name: Create Release (production)
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
      - name: Install dependencies
        run: npm ci
      - name: Verify package signatures
        run: npm audit signatures
      - name: Create release for production
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # <!-- BEGIN RBP GENERATED: ci-branch-autodeploy-v1 -->
          # Production: standard semantic-release (no prerelease).
          # <!-- END RBP GENERATED: ci-branch-autodeploy-v1 -->
          npx semantic-release

  deploy-shopify:
    name: Deploy to Shopify (production)
    needs: [release]
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 'lts/*'
          cache: 'npm'
      - name: Install dependencies
        run: npm ci
      - name: Install Shopify CLI
        run: npm install -g @shopify/cli@latest
      - name: Build
        run: npm run build
        env:
          NODE_ENV: production
      - name: Deploy to Shopify (production)
        env:
          SHOPIFY_CLI_PARTNERS_TOKEN: ${{ secrets.SHOPIFY_CLI_PARTNERS_TOKEN }}
          COMMIT_URL: ${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }}
        run: |
          # <!-- BEGIN RBP GENERATED: ci-branch-autodeploy-v1 -->
          # Production uses default app config.
          # <!-- END RBP GENERATED: ci-branch-autodeploy-v1 -->
          shopify app deploy -f --source-control-url "$COMMIT_URL"

  deploy-fly:
    name: Deploy to Fly (production)
    needs: [deploy-shopify]
    runs-on: ubuntu-latest
    environment: production
    concurrency:
      group: deploy-production
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@v4
      - uses: superfly/flyctl-actions/setup-flyctl@master
      - name: Deploy to Fly (production)
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN_PROD }}
        run: |
          # <!-- BEGIN RBP GENERATED: ci-branch-autodeploy-v1 -->
          # Production Fly app config
          # <!-- END RBP GENERATED: ci-branch-autodeploy-v1 -->
          flyctl deploy --remote-only --config fly.production.toml
