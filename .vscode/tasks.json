{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Typecheck and build",
      "type": "shell",
      "command": "npm run -s typecheck && npm run -s build",
      "group": "build"
    },
    {
      "label": "Build app",
      "type": "shell",
      "command": "npm run -s build",
      "problemMatcher": ["$tsc"],
      "group": "build"
    }
  ]
}
